cookie:
	domain: ADD HERE # or leave blank for default (host fqdn)
	duration: ADD HERE # in secs, or leave blank for default (~ 1 year)
	p3p_header: ADD HERE # or leave blank for default
sink: # What sink will we use to collect events?
	out: redirect # or change to 'none' if using Tomcat logging. 'fluentd', 'stdout' not supported yet.
	redirect:
		url: ADD HERE # or leave blank if no redirect
		attach_uid: true # or change to 'false'